# Git Auto-Push Rule

Whenever any source code or project file is modified, updated, or fixed in this repository:
1. Always compile and verify the build if code changes were made.
2. Automatically commit all changes with a clear, descriptive git commit message.
3. Automatically push the commit to `https://github.com/thaidelphi/android_microphone` on branch `main` (`git push origin main`).
